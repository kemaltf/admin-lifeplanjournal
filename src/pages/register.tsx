import Head from "next/head";

type Props = {};

const Register = (props: Props) => {
  return (
    <div>
      <Head>
        <title>Register</title>
      </Head>
      <h1>Register</h1>
    </div>
  );
};

export default Register;
