import Layout from "@/components/organism/Layout";
import React from "react";

type Props = {};

const Products = (props: Props) => {
  return <Layout>Here is product page</Layout>;
};

export default Products;
